name: Temporary repository audit

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build repository inventory
        run: |
          git ls-files | sort > tracked-files.txt
          {
            echo "=== SCRIPTS ==="
            find scripts -maxdepth 3 -type f -print 2>/dev/null | sort || true
            echo
            echo "=== WORKFLOWS ==="
            find .github/workflows -maxdepth 1 -type f -print | sort
            echo
            echo "=== ROOT HTML / NOTES / BACKUPS ==="
            find . -maxdepth 3 -type f \( -name '*.html' -o -name '*NOTE*' -o -name '*BACKUP*' -o -name '*.bak' -o -name '*.tmp' \) -print | sort
            echo
            echo "=== PACKAGE SCRIPTS ==="
            node -e "console.log(JSON.stringify(require('./package.json').scripts, null, 2))"
            echo
            echo "=== MODEL / GENERATION REFERENCES ==="
            grep -RInE 'generate|generator|one[- ]off|temporary|backup|preview|helper|convert|embed|optimi[sz]e' scripts src public workers .github 2>/dev/null | head -2000 || true
          } > repository-audit.txt

      - name: Upload repository inventory
        uses: actions/upload-artifact@v4
        with:
          name: repository-audit
          path: |
            tracked-files.txt
            repository-audit.txt
          if-no-files-found: error
          retention-days: 1
